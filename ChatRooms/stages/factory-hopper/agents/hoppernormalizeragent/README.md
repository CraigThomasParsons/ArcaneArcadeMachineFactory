# HopperNormalizerAgent

Stage: factory-hopper

Custom logic hook:
- Implement `transform(job, package_dir, out_dir)` in `agent.py`.
- Return a list of relative artifact paths to append into artifact_refs.
