# Sprint1 Handoff (2026-03-18)

Sprint: Sprint1 - Stage Contracts And Filesystem Foundations
Status: Complete

## What Is Complete

1. Stage contract and runtime foundation are in place.
2. Event publishing and projection artifacts are present.
3. Rollout parity and cutover handoff package artifacts are documented.
4. Bridge and Godot-side integration anchors exist and are wired for local flow.
5. Task 4 hardening established a state-first boundary in chat scene logic.

## Compatibility Verification

1. Core integration artifacts verified present:
- `TheFactoryHopper/bin/stage_workers.py`
- `TheFactoryHopper/bin/stage_runtime.py`
- `TheFactoryHopper/bin/arcane_event_contract.py`
- `bridge/ws_broker.py`
- `game/chatroom_test_scene.gd`

2. Rollout and recovery docs verified present:
- `TheFactoryHopper/docs/event_rollout/handoff_package.md`
- `TheFactoryHopper/docs/event_rollout/parity_report.json`
- `TheFactoryHopper/docs/recovery_runbook.md`

3. Sprint task set verified present:
- `docs/sprint_tasks/Sprint1/0-tasks.md` through `docs/sprint_tasks/Sprint1/5-tasks.md`

## Downstream Dependencies (Sprint2+)

1. Maintain contract compatibility for event envelope fields when extending event types.
2. Preserve projection determinism when adding new reducers.
3. Keep bridge payload shape compatible with chatroom consumer expectations.
4. Extend Task 4 validation approach to new conversation/state features.

## Open Questions

1. Should persisted conversation history remain user-local only or be mirrored to stage artifacts?
2. Which actor should own schema evolution policy once Sprint2 introduces new event variants?
3. Do we want a formal smoke-check command that validates runtime + bridge + chatroom together?

## Risks

1. Runtime queue directories can grow quickly if cleanup cadence is not defined.
2. Schema drift risk if producers and consumers evolve independently.
3. Godot UI behavior may diverge from bridge event semantics without integration tests.

## Next Actions

1. Start Sprint2 Task 0 scope lock and metrics using Sprint1 outputs as baseline.
2. Add repeatable integration smoke check script for runtime-bridge-chatroom path.
3. Define schema change protocol and versioning guardrails before new event families are introduced.
